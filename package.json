{
  "name": "visual-agents",
  "version": "0.4.0",
  "description": "Pixel art visualization of Claude Code agents working in real-time",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:webview": "vite build --config vite.config.extension.ts",
    "build:ext": "cd extension && npx tsc -p .",
    "build:extension": "npm run build:webview && npm run build:ext",
    "package:vsix": "npm run build:extension && cd extension && npx vsce package --no-dependencies",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "easystarjs": "^0.4.4"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
